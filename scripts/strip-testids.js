// scripts/strip-testids.js
// Production build step placeholder: no test-id stripping is performed.
// Exists so `npm run build:prod` (used by the build-app CI job) succeeds.
console.log("build:prod — strip-testids no-op (nothing to strip).");
process.exit(0);
