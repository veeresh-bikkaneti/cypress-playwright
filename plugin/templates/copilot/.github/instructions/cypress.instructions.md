---
applyTo: "cypress/**"
---

This tree is **migration source**, not a place to add new Cypress tests.

When the user asks to convert a spec or command, load `skills/cypress-to-playwright-migration` and write Playwright under `playwright/`. Do not expand Cypress coverage unless they explicitly ask to keep Cypress.
